{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "Phonebook backend application for fullstackopen course",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -r dist && cd ../fullstackopen/part2/phonebook && npm run build && cp -r dist ../../../fullstackopen_pt3",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Matt Rodgers",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^3.1.7"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "morgan": "^1.10.0"
  }
}
